---
# tasks file for roles/apache
- name: Establishing Apache
  apt:
    name: apache2
    state: latest
    update_cache: yes

- name: Setup for firewall to apache
  ufw:
    rule: allow
    direction: in
    name: 'Apache'
