---
- name: Install PHP
  apt:
   name:
    - php
    - libapache2-mod-php
    - php-mysql

- name: Verifying the PHP version
  shell: php -v
  register: phpv

- name: Results
  debug:
   msg: "{{ phpv.stdout }}"

# tasks file for roles/installphp